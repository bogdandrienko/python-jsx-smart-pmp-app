import React from "react";
import { BaseComponent1 } from "../components/ui/base";

export const AboutPage = () => {
  // const myVar = "Nick";
  // const myVar = "John"; // выведет ошибку, объявить другое значение нельзя
  // const person = {
  //   name: "Nick",
  // };
  // person.name = "John"; // будет работать, поскольку значение изменили, а не присвоили заново
  // console.log(person.name); // "John"
  // person = "Sandra"; // выведет ошибку, поскольку повторное присвоение не доступно для переменных,
  // // объявленных с помощью const

  return (
    <main className="text-center">
      <h1>About Page</h1>
      <div>123</div>
    </main>
  );
};
